'use strict'

const api = require('./api')
const ui = require('./ui')

const onUserSelection = function (event) {
  console.log(event)
  const gameIndex = ['', '', '', '', '', '', '', '', '']
}

module.exports = {
  // onSignUp: onSignUp
  onUserSelection
}
